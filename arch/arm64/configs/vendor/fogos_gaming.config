###############################################################################
# FogOS Extreme Gaming Kernel - Gaming Optimization Fragment
# Device: Motorola G45 (SM6375 / Holi)
# Developer: Prince (VirgoYT707)
# Kernel Base: 5.4.302
# Purpose: Maximum BGMI gaming performance with lowest possible latency
###############################################################################

###############################################################################
# BRANDING
###############################################################################
CONFIG_LOCALVERSION="-G45-Gaming"

###############################################################################
# CPU - Performance Scheduler & Boost
###############################################################################

# Full preemption for lowest latency
# CONFIG_PREEMPT_NONE is not set
# CONFIG_PREEMPT_VOLUNTARY is not set
CONFIG_PREEMPT=y

# WALT scheduler (already present, ensure tuning options are on)
CONFIG_SCHED_WALT=y
CONFIG_SCHED_CASS=y

# schedutil governor (adaptive, works well with WALT)
CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
CONFIG_CPU_FREQ_GOV_PERFORMANCE=y
CONFIG_CPU_FREQ_GOV_POWERSAVE=y
CONFIG_CPU_FREQ_GOV_USERSPACE=y
CONFIG_CPU_FREQ_GOV_ONDEMAND=y
CONFIG_CPU_FREQ_GOV_CONSERVATIVE=y

# uclamp task clamping for foreground boost
CONFIG_UCLAMP_TASK=y
CONFIG_UCLAMP_TASK_GROUP=y
CONFIG_UCLAMP_BUCKETS_COUNT=20

# High resolution timers (lower latency wakeup)
CONFIG_HIGH_RES_TIMERS=y
CONFIG_NO_HZ=y

# IRQ time accounting
CONFIG_IRQ_TIME_ACCOUNTING=y

# SMP nice spreading for gaming (use big cores)
CONFIG_SCHED_MC=y
CONFIG_SCHED_SMT=y

# Reduce scheduling jitter
CONFIG_HZ_300=y
# CONFIG_HZ_100 is not set
# CONFIG_HZ_250 is not set

###############################################################################
# GPU - Adreno Performance
###############################################################################

# Adreno GPU devfreq governors
CONFIG_DEVFREQ_GOV_QCOM_ADRENO_TZ=y
CONFIG_DEVFREQ_GOV_QCOM_GPUBW_MON=y
CONFIG_QCOM_KGSL=y
CONFIG_QCOM_KGSL_CONTEXT_DEBUG=y

###############################################################################
# VIDEO HARDWARE DECODE — fixes Instagram Reels / YouTube Shorts / video calls
# Venus is the Qualcomm Video Acceleration Engine (hardware decoder/encoder)
# Without this, video decoding falls back to CPU = stutter, loading loops, lag
###############################################################################
CONFIG_VIDEO_QCOM_VENUS=y
CONFIG_VIDEO_QCOM_VENUS_CORE0=y
# Required media framework plumbing
CONFIG_MEDIA_SUPPORT=y
CONFIG_VIDEO_V4L2=y
CONFIG_V4L2_MEM2MEM_DEV=y
CONFIG_VIDEOBUF2_DMA_CONTIG=y
CONFIG_VIDEOBUF2_V4L2=y

# HDCP — required for Widevine L1 (HD Netflix, Amazon Prime, Hotstar)
CONFIG_HDCP_QSEECOM=y

###############################################################################
# MEMORY - LMK & VM Optimization
###############################################################################

# LRU_GEN for better memory reclaim (modern MGLRU)
CONFIG_LRU_GEN=y
CONFIG_LRU_GEN_ENABLED=y

# Android LMKD support
CONFIG_ANDROID_LOW_MEMORY_KILLER=y

# Balance anonymous vs file page reclaim
CONFIG_BALANCE_ANON_FILE_RECLAIM=y

# Prioritize OOM tasks
CONFIG_PRIORITIZE_OOM_TASKS=y

# ZRAM with ZSTD (fastest + best ratio for gaming)
CONFIG_ZRAM=y
CONFIG_ZRAM_DEF_COMP_ZSTD=y
CONFIG_CRYPTO_ZSTD=y
CONFIG_ZSMALLOC=y
CONFIG_ZSMALLOC_STAT=y

# CMA for contiguous memory allocation
CONFIG_CMA=y

# Transparent huge pages for better memory throughput
CONFIG_TRANSPARENT_HUGEPAGE=y
CONFIG_TRANSPARENT_HUGEPAGE_ALWAYS=y
# CONFIG_TRANSPARENT_HUGEPAGE_MADVISE is not set

# Reduce memory fragmentation
CONFIG_COMPACTION=y
CONFIG_MIGRATION=y

# Faster memory allocation path
CONFIG_SLUB=y
CONFIG_SLUB_CPU_PARTIAL=y

###############################################################################
# I/O SCHEDULER - BFQ Tuned for Low Latency
###############################################################################

CONFIG_IOSCHED_BFQ=y
CONFIG_BFQ_GROUP_IOSCHED=y
CONFIG_DEFAULT_IOSCHED="bfq"
CONFIG_MQ_IOSCHED_DEADLINE=y
CONFIG_MQ_IOSCHED_KYBER=y

# UFS (storage) optimizations
CONFIG_SCSI_UFSHCD=y
CONFIG_SCSI_UFS_BSG=y
CONFIG_SCSI_UFSHCD_QTI=y

# Block layer tuning
CONFIG_BLK_DEV_THROTTLING=y
CONFIG_BLK_WBT=y

###############################################################################
# NETWORK - Low Latency TCP
###############################################################################

# TCP BBR congestion control (best for gaming/low latency)
CONFIG_TCP_CONG_BBR=y
CONFIG_TCP_CONG_BBRPLUS=y
CONFIG_DEFAULT_BBR=y
CONFIG_NET_SCH_FQ=y
CONFIG_NET_SCH_FQ_CODEL=y

# UDP busy-poll for low-latency game socket wakeup
CONFIG_NET_RX_BUSY_POLL=y

###############################################################################
# AUDIO — Full Qualcomm QDSP6 stack + Dolby Atmos
###############################################################################

# QDSP6 v2 Audio Framework
CONFIG_SND_SOC_MSM_QDSP6V2_INTF=y
CONFIG_SND_SOC_QDSP6V2=y

# Post-processing — Dolby Atmos / EQ / effects
CONFIG_QTI_PP=y
CONFIG_SND_HWDEP_ROUTING=y

# Stub codec (required by QDSP6 framework)
CONFIG_SND_SOC_MSM_STUB=y

# Bolero codec — macro-based codec for Holi platform
CONFIG_SND_SOC_BOLERO=y
CONFIG_VA_MACRO=y
CONFIG_RX_MACRO=y
CONFIG_TX_MACRO=y
CONFIG_DIGITAL_CDC_RSC_MGR=y

# SoundWire — codec interconnect bus
CONFIG_SOUNDWIRE=y
CONFIG_SOUNDWIRE_MSTR_CTRL=y
CONFIG_REGMAP_SWR=y

# WCD codec variants — Motorola G45 uses WCD937x
# NOTE: REGMAP_WCD_IRQ and MFD_WCD must be enabled in defconfig (base config)
CONFIG_SND_SOC_WCD937X=y
CONFIG_SND_SOC_WCD937X_SLAVE=y
CONFIG_SND_SOC_WCD938X=y
CONFIG_SND_SOC_WCD938X_SLAVE=y
CONFIG_SND_SOC_WCD_IRQ=y

# WSA speaker amp + macro
CONFIG_WSA_MACRO=y
CONFIG_SND_SOC_WSA881X_ANALOG=y
CONFIG_WSA881X_TEMP_SENSOR_DISABLE=y

# Headset / headphone jack detection
CONFIG_WCD_MBHC=y
CONFIG_WCD_MBHC_ADC=y

# Holi platform audio machine driver — ties everything together
CONFIG_SND_SOC_HOLI=y
CONFIG_SND_EVENT=y

# SLIMBUS — BT audio transport
CONFIG_BTFM_SLIM=y
CONFIG_SLIMBUS=y
CONFIG_SLIM_QCOM_CTRL=y
CONFIG_SLIM_QCOM_NGD_CTRL=y
CONFIG_SLIMBUS_MSM_CTRL=y

# Disable unused interfaces to keep build clean
CONFIG_TDM_DISABLE=y
CONFIG_AUXPCM_DISABLE=y

# Motorola speaker amp (FS1815 — already in defconfig)
CONFIG_SND_SOC_FS1815=y
